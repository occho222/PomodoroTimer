<Window x:Class="PomodoroTimer.Views.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding DialogTitle}" 
        Height="280" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        MinHeight="250" MinWidth="450"
        Background="{StaticResource BackgroundLight}">

    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- メッセージ -->
        <TextBlock Grid.Row="0" Text="{Binding Message}" 
                   FontSize="14" Margin="0,0,0,20" 
                   TextWrapping="Wrap" Foreground="{StaticResource TextLight}"/>

        <!-- 入力フィールド -->
        <TextBox Grid.Row="1" x:Name="InputTextBox" 
                 Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}"
                 FontSize="14" Padding="12,8" Height="40" Margin="0,0,0,20"
                 Background="White" BorderBrush="#E0E0E0" BorderThickness="1"
                 VerticalContentAlignment="Center"/>

        <!-- ボタン -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" 
                    HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="OK" Width="90" Height="35" Margin="0,0,10,0"
                    Click="OkButton_Click"
                    Style="{StaticResource RoundButton}" 
                    Background="#3B82F6" Foreground="White"
                    FontSize="13" FontWeight="SemiBold"/>
            <Button Content="キャンセル" Width="90" Height="35"
                    Click="CancelButton_Click"
                    Style="{StaticResource RoundButton}" 
                    Background="#6B7280" Foreground="White"
                    FontSize="13" FontWeight="SemiBold"/>
        </StackPanel>
    </Grid>
</Window>